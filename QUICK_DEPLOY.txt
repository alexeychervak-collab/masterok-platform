STROYKA - Quick Deploy

If GitHub push fails, use direct SCP:

On Windows:
cd d:\yodo
tar -czf stroyka-clean.tar.gz backend landing mobile README.md LICENSE
scp stroyka-clean.tar.gz Tema@188.68.223.230:~/

On Server:
cd ~
tar -xzf stroyka-clean.tar.gz
cd backend
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
echo 'DATABASE_URL=sqlite:///./stroyka.db' > .env
uvicorn app.main_simple:app --host 0.0.0.0 --port 8000

